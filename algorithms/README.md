## **NOTE**

- This folder contains standalone implementation of algorithms. 
- Redundant now, training/run_td_control is the unified training loop. 
- Algorithm implementations reside in /agents directory. 

- This directory will rarely be updated, even if updated, will be standalone implementation of algorithms just for testing or reference.